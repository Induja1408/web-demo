{
  "name": "web-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "zip": "rm -f dist.zip && cd src && zip -r ../dist.zip ."
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest-junit": { "outputDirectory": "reports", "outputName": "junit.xml" }
}
